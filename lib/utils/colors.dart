import 'package:flutter/material.dart';

// Colors for mobile layout
const mobileBackgroundColor = Colors.white; // Light background
const mobileSearchColor = Colors.grey; // Dark search color

// Colors for web layout
const webBackgroundColor = Colors.white; // Light background

// App-specific colors
const blueColor = Color.fromRGBO(0, 149, 246, 1);
const primaryColor = Colors.black; // Dark text color
const secondaryColor = Color.fromARGB(255, 143, 143, 143); // Grey accent color
